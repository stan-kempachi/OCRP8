language: python
python:
          - '3.6.9'

before_script:
          - pip install -r requirements.txt

env: DJANGO_SETTINGS_MODULE="plateforme_project.settings"

services:
          - postgresql

script:
          - python manage.py test
branches:
   only:
          - master
